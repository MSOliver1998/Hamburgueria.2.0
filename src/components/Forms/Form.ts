import styled from "styled-components";

const FormStyled=styled.form`
    width: 500px;
    margin: 0 auto;

    div{
        margin: 0 auto;
        width: 500px;
        display: flex;
        flex-direction: column;
    }
`

export {FormStyled}